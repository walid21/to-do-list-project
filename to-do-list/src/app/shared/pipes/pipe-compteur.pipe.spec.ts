import { PipeCompteurPipe } from './pipe-compteur.pipe';

describe('PipeCompteurPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCompteurPipe();
    expect(pipe).toBeTruthy();
  });
});
